
#include <com/github/doevelopper/atlas/launcher/fsm/Idle.hpp>

using namespace com::github::doevelopper::atlas::launcher::fsm;

Idle::Idle() noexcept
{
    // if (context<ApplicationStateMachine>())
    // if (context<ApplicationStateMachine>().stateChangeCallback)
    // {
    //     context<ApplicationStateMachine>().stateChangeCallback("Idle");
    // }
    // std::cout << "Application is idle and ready to initialize" << std::endl;
}

